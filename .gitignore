# ───────────────────────────────────────────────────────────────
# Ionic + Capacitor + Vue + Node.js
# ───────────────────────────────────────────────────────────────

# Abhängigkeiten
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
bun.lockb

# Build- & Dist-Ordner
/dist/
/www/
/build/
/dist-electron/
/coverage/

# Ionic / Capacitor generierte Dateien
/android/
/ios/
/capacitor.config.*
capacitor.config.json    # ← oft gewollt – aber manche Teams committen ihn
*.keystore
*.jks
*.apk
*.aab
*.ipa

# Lokale Entwicklung & temporäre Dateien
*.log
*.local
*.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
desktop.ini

# Editor / IDE Dateien
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.~*

# Umgebung & sensible Daten
.env
.env.*
!.env.example
config/*.secret.*
credentials.json

# TypeScript / Vue temporäre Dateien
*.tsbuildinfo
*.vuejs.*
vite.config.*.local.*
vue.config.local.*

# Testing & Coverage
.nyc_output/
test-results/
playwright-report/
blob-report/

# macOS / Windows Kram
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Capacitor Live Reload / Dev Server Cache
.capacitor/

# Optional – wenn du den capacitor.config nicht committen willst
# capacitor.config.*

# ───────────────────────────────────────────────────────────────
# Häufig gewollt committet – aber manchmal ignoriert
# ───────────────────────────────────────────────────────────────
# package-lock.json     ← meist committen
# yarn.lock             ← meist committen
# pnpm-lock.yaml        ← meist committen
# capacitor.config.json ← meist committen (seit Ionic 6/7 empfohlen)